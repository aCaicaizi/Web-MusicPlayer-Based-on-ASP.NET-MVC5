@model string
@{
    ViewBag.Title = "Search";
    Layout = "~/Views/Shared/Player_Layout.cshtml";
}
<script src="~/Scripts/jquery-3.6.0.min.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<h2>Search</h2>
@Html.Action("ArtistOrAlbum", new { s=Model})
<div class="card text-center">
    <div class="card-header">
        <ul class="nav nav-tabs card-header-tabs">
            <li class="nav-item">
                @Ajax.ActionLink("单曲", "MusicSearch", "Music", new { s = Model }, new AjaxOptions { UpdateTargetId = "cardBody", HttpMethod = "GET", InsertionMode = InsertionMode.Replace }, new { @class = "nav-link active", aria_current = "true" })
            </li>
            <li class="nav-item">
                @Ajax.ActionLink("专辑", "AlbumSearch", "Music", new { s = Model }, new AjaxOptions { UpdateTargetId = "cardBody", HttpMethod = "GET", InsertionMode = InsertionMode.Replace }, new { @class = "nav-link" })

            </li>
            @*<li class="nav-item">
            @Ajax.ActionLink("MV", "MusicSearch", "Music", new {s=Model }, new AjaxOptions { UpdateTargetId = "cardBody", HttpMethod = "GET", InsertionMode = InsertionMode.Replace }, new { @class = "nav-link" })

        </li>*@
            @*<li class="nav-item">
            @Ajax.ActionLink("歌单", "MusicSearch", "Music", new {s=Model }, new AjaxOptions { UpdateTargetId = "cardBody", HttpMethod = "GET", InsertionMode = InsertionMode.Replace }, new { @class = "nav-link" })

        </li>*@
            <li class="nav-item">
                @Ajax.ActionLink("用户", "UserSearch", "User", new { s = Model }, new AjaxOptions { UpdateTargetId = "cardBody", HttpMethod = "GET", InsertionMode = InsertionMode.Replace }, new { @class = "nav-link" })

            </li>
            <li class="nav-item">
                @Ajax.ActionLink("歌词", "LyricsSearch", "Music", new { s = Model }, new AjaxOptions { UpdateTargetId = "cardBody", HttpMethod = "GET", InsertionMode = InsertionMode.Replace }, new { @class = "nav-link" })

            </li>
        </ul>
    </div>

    <div class="card-body" id="cardBody">
        @{Html.RenderAction("MusicSearch", new { s = Model });}
    </div>
</div>
<script type="text/javascript">

</script>